[pytest]
pythonpath = .
testpaths = tests
addopts = -ra --strict-markers
markers =
    smoke: fast, critical-path checks (run first, gate the build)
    regression: broader regression coverage
    auth: authentication / session behaviour
    crud: create/read/update/delete lifecycles
    negative: negative and error-path cases
    tenant: multi-tenant isolation
